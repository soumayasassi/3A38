
<table border="1">
    <tr>
        <td>NSC</td>
        <td>Email</td>
        <td>Moyenne</td>
        <td>Class</td>
    </tr>
        {% for student in students %}
    <tr>
        <td>{{ student.nsc }}</td>
        <td>{{ student.email }}</td>
        <td>{{ student.moyenne }}</td>
        <td>{{ student.class.name }}</td>
    </tr>
    {% endfor %}
</table>

